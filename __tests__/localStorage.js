/*
|--------------------------------------------------------------------------
| storage test suite
|--------------------------------------------------------------------------
|
| This tests suite makes sure that the current Cart works correctly with
| a mock localStorage passed as storage object.
|
| It's basically to make sure that the storage connector that should
| be passed down on initialization is working correctly.
*/

// To do
// - Write Jest mock for localStorage
// - Write a test for get, save, and clear.
